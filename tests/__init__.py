"""Tests for AccessibleIDE.

Uses only the standard library's unittest, so the project needs no
test dependencies installed. Run from the repo root with:

    PYTHONPATH=src python -m unittest discover -s tests -t .
"""
